# ============================================================
# Render Blueprint - 一键部署
# 用法: https://render.com/deploy?repo=https://github.com/Yozeawa/orange-shop
# ============================================================
services:
  - type: web
    name: orange-shop
    runtime: node
    repo: https://github.com/Yozeawa/orange-shop
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/products
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ALLOWED_ORIGIN
        value: https://orange-shop-rrl.pages.dev
      - key: NODE_VERSION
        value: 20
